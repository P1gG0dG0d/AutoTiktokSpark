# 📕 抖音自动续火花 · 使用说明

> 本工具每晚自动给你的抖音好友发送一条你设定的话，
> 让你们的火花永远不会断。全部自动，无需你做任何事。

---

## 一、平时你什么都不用做 ✅

每天晚上 **20:00** 电脑自动开始工作（随机等 0~55 分钟后发送，更像真人），
发送成功后会自动截图存证据，然后关闭浏览器。你睡觉、看电视都不受影响。

**唯一的前提：这台电脑要开着、登录着 Windows（锁屏没关系），不能关机、不能睡眠。**

---

## 二、文件夹里的文件都是干嘛的

| 文件/文件夹 | 作用 | 你需要动它吗 |
|---|---|---|
| `扫码登录.bat` | 抖音登录失效时，双击它重新扫码 | 偶尔（登录过期时） |
| `立即测试发送.bat` | 想立刻测试一次就双击它 | 想测试时用 |
| `一键安装定时任务.bat` | 安装"每晚20点自动运行"的计划（装一次就行） | 只装一次 |
| `设置电脑永不睡眠.bat` | 让电脑不睡觉（装一次就行） | 只装一次 |
| `config.json` | 配置：发给谁、发什么、几点发 | 想改内容时找助手改 |
| `screenshots/` | 每次发送成功的截图证据 | 只看不动 |
| `logs/` | 运行日志 | 出问题时给助手看 |
| `browser_profile/` | 浏览器登录状态（**删了就要重新扫码！**） | ⚠️ 千万别删 |
| `latest_status.json` | 最近一次的运行结果 | 可以打开看 |

---

## 三、怎么知道今天发送成功了？

用记事本打开 `latest_status.json`，看到：

```
"result": "SUCCESS"
```

就是成功了。也可以进 `screenshots/` 文件夹看截图，能看到消息发出去的画面。

---

## 四、常见问题

### ❓ 黑窗口显示「登录已过期」/ latest_status.json 里是 NEED_LOGIN
→ 双击 `扫码登录.bat`，手机扫码，看到"✔ 登录成功"即可。第二天就恢复正常。

### ❓ 电脑重启过了，还会自动发吗？
→ 会。定时任务一直有效，只要电脑开着到晚上 20 点就行。

### ❓ 想换发送内容 / 换好友
→ 打开 `config.local.json`（没有就新建一个），改 `"friend_name"`（好友昵称）
或 `"message"`（发送内容）引号里的内容，保存即可。
这个文件存的是你的隐私（好友名、消息），不会被上传到 GitHub。
（或者直接找助手帮忙改。）

### ❓ result 是 FAILED 怎么办？
→ 打开 `logs/` 里最新月份的日志看看最后几行，并把 `logs/`、`screenshots/`
文件夹的情况告诉助手，会自动诊断修复。脚本失败当晚会自动重试 2 次。

### ❓ 想暂时停用几天
→ 在开始菜单搜"任务计划程序"，找到 `AutoTiktokSpark_Huohua`，右键"禁用"。
想恢复就右键"启用"。

---

## 五、注意事项 ⚠️

1. **别删 `browser_profile` 文件夹**，也别在弹出的浏览器里点"退出登录"，
   否则就要重新扫码。
2. 抖音账号建议只在这一台电脑上这样用，别用别的工具同时自动化同一个号。
3. 本工具每天只发 1 条消息，模拟真人节奏，风控风险很低，但请知悉任何自动化
   操作都无法保证 100% 无风险。
